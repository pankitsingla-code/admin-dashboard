specVersion: "v1"
serviceId: "admin-dashboard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "admin-dashboard-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
